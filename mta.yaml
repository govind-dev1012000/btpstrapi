ID: nextapp1
_schema-version: "2.1"
version: 0.0.1
   
build-parameters:
  before-all:
   - builder: custom
     commands:
      - npm ci --only=production


modules:
  - name: nextapp1
    path: .
    type: nodejs
    # build-parameters:
    #   builder: custom
    #   commands:
    #     - npm run build
    parameters:
      memory: 1024M
      disk-quota: 1024M
      parameters:
      - tasks:
          - name: task-1
            command: npm start
      # build-parameters:
      #   commands:
      #   - npm start
        supported-platforms: [CF]