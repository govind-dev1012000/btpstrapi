ID: nextapp1
_schema-version: "2.1"
version: 0.0.1
   
build-parameters:
  before-all:
   - builder: custom
     commands:
      - npm ci


modules:
  - name: nextapp1
    path: .
    # type: nodejs
    # build-parameters:
    #   builder: custom
    #   commands:
    #     - npm run build
    parameters:
      memory: 1024M
      disk-quota: 1024M
      buildpack: nodejs_buildpack
      build-parameters:
        builder: npm-ci
        commands:
        - npm run build
        supported-platforms: [CF]