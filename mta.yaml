ID: straipsapp1
_schema-version: "2.1"
version: 0.0.1

# build-parameters:
#   before-all:
#    - builder: custom
#      commands:
#       - npm install
#       - npm run build


modules:
  - name: straipsapp1
    path: .
    type: nodejs
    parameters:
      memory: 512M
      disk-quota: 1024M
    
    # build-parameters:
    #   builder: custom
    #   commands:
    #   - npm install
    #   - npm run build
    #   supported-platforms: [CF]